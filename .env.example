# Used by docker-compose.yml to fill in container environment variables.
# Copy to .env in the project root and fill in real values before running:
#   docker compose up --build

# --- Database ---
DB_USER=pizzeria_user
DB_PASSWORD=changeme
DB_NAME=pizzeria_db

# --- Email (SMTP) — see server/.env.example for provider-specific notes ---
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM="Nithish Pizzeria <no-reply@nithishpizzeria.com>"
