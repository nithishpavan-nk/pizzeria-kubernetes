apiVersion: v1
kind: Secret

metadata:
  name: postgres-secret
  namespace: pizzeria

type: Opaque

stringData:
  POSTGRES_USER: pizzeria_user
  POSTGRES_PASSWORD: changeme
  POSTGRES_DB: pizzeria_db
